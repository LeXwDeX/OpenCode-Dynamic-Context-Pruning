# 媒体与小输出剪枝实验：保留现行默认策略

本次结论是 **保留媒体出现时整次放行的策略，保留单项最小收益默认值 512，不增加聚合收益分支**。实验说明了现行策略的代价，也证明已有参数能够改善本次小输出样例；没有证据证明扩大自动清理范围能提高真实任务质量。

对应议题：[媒体策略 #48](https://github.com/LeXwDeX/OpenCode-Dynamic-Context-Pruning/issues/48)、[小输出收益 #49](https://github.com/LeXwDeX/OpenCode-Dynamic-Context-Pruning/issues/49)。本交付仅增加离线实验及决策记录，没有修改生产引擎、默认配置或用户活动环境。

## 复现与计量范围

在安装了项目开发依赖的仓库根目录运行：

```sh
node --import tsx scripts/experiments/pruning-policy.mjs > /tmp/dcp-pruning-policy-results.json
```

脚本调用当前 `lib/dtc/engine.ts` 的 `projectMessages` 和 `estimateMessages`；任一保护断言失败会非零退出，正常 stdout 是一个完整 JSON 文档。没有调用宿主、模型服务或修改会话存储。

固定输入预算为 40,000，目标为 `40,000 × 0.7 = 28,000`。保留默认的至少 4 个完整工具步骤及至少 16,000 估算 tokens 的近期保护。每个助手消息有一个原生 `step-start` / `step-finish` 步骤，并保留文本、推理、完整工具输入和调用 ID；保护对照的最后一步还包含两个并行工具结果。这里只将独立请求副本上的成功旧工具结果加上宿主原生 `compacted` 标记。

输出是重复 ASCII 字符，数字来自生产估算器，并非模型 tokenizer 或模型服务的硬窗口测量。输入、调用 ID、文本和结构开销均计入，故与只计算输出字符的旧审计样例不完全相同。

脚本比较三种方案：

| 方案                      | 单个输出门槛 | 额外规则                                                          |
| ------------------------- | -----------: | ----------------------------------------------------------------- |
| `default-512`             |          512 | 当前默认                                                          |
| `configured-128`          |          128 | 通过现有 `dtc.minimumSavingsTokens` 配置实现                      |
| `aggregate-512-prototype` |            1 | 先使用现有引擎选出独立候选，只有累计净收益达到 512 才采纳这次投影 |

聚合原型不合并工具调用、不删除消息，也不绕过任何候选或近期保护。它只存在于实验脚本中。`normal` 到达目标即停止；`force` 继续考虑全部合格旧输出，仍受相同保护和收益门槛约束。

## 小输出：降低已有门槛有效，但不能保证长期达标

`small-2000` 的每个输出有 2,000 ASCII 字符，估算 500 tokens；替换为原生清理占位后净节省 **491**，未达到默认 512。`mixed-2000-12000` 每十步中有一步输出 12,000 字符，其余仍为 2,000 字符。

下表为普通请求；“结果”表示剩余估算 tokens / 折叠工具数。本矩阵中配置 128 与聚合原型产生相同结果。

| 输出 / 步数  | 原始估算 | 默认 512 结果 | 配置 128 / 聚合结果 | 完整近期保护步骤 | 后两方案达到 28,000 |
| ------------ | -------: | ------------: | ------------------: | ---------------: | ------------------- |
| 小输出 / 20  |   11,121 |    11,121 / 0 |          11,121 / 0 |               20 | 是，原本就未超出    |
| 小输出 / 100 |   55,521 |    55,521 / 0 |         27,534 / 57 |               29 | 是                  |
| 小输出 / 300 |  166,721 |   166,721 / 0 |        33,660 / 271 |               29 | 否                  |
| 混合 / 20    |   16,121 |    16,121 / 0 |          16,121 / 0 |               20 | 是，原本就未超出    |
| 混合 / 100   |   80,521 |    56,593 / 8 |         27,669 / 72 |               20 | 是                  |
| 混合 / 300   |  241,721 |  157,973 / 28 |        34,241 / 280 |               20 | 否                  |

100 步小输出的配置 128 方案节省 27,987；100 步混合输出节省 52,852。默认方案在混合样例中仅折叠大输出，其余小输出仍积累。

300 步的两种样例，即使折叠全部合格旧输出，仍分别剩余 33,660 和 34,241。完整输入、调用关系、文本、推理、原生清理占位以及受保护近期结果继续占用空间。增加聚合分支没有解决这部分压力，不能把“能清理更多输出”表述为“上下文可以无限增长”。

`force` 对照如下；20 步样例全部位于近期保护范围，三种方案都不折叠。

| 输出 / 步数  | 默认 512：剩余 / 折叠数 | 配置 128 / 聚合：剩余 / 折叠数 |
| ------------ | ----------------------: | -----------------------------: |
| 小输出 / 100 |              55,521 / 0 |                    20,660 / 71 |
| 小输出 / 300 |             166,721 / 0 |                   33,660 / 271 |
| 混合 / 100   |              56,593 / 8 |                    21,241 / 80 |
| 混合 / 300   |            157,973 / 28 |                   34,241 / 280 |

### 聚合门槛的额外价值与代价

为避免只用“两个方案恰好相同”的样例作结论，脚本另加 `force` 对照：旧输出之后始终保留八个 8,000 字符的完整近期步骤。

| 合格旧输出      | 默认 512：节省 / 折叠数 | 配置 128 | 聚合门槛 512 | 现有配置 1 |
| --------------- | ----------------------: | -------: | -----------: | ---------: |
| 一份 2,000 字符 |                   0 / 0 |  491 / 1 |        0 / 0 |    491 / 1 |
| 两份 2,000 字符 |                   0 / 0 |  982 / 2 |      982 / 2 |    982 / 2 |
| 64 份 120 字符  |                   0 / 0 |    0 / 0 |   1,344 / 64 | 1,344 / 64 |

聚合规则能够保留累计不足 512 的小批次，也能够清理低于 128 的多个微小输出。但最后一行需要从当前模型请求隐藏 64 份完整结果，合计只减少 1,344 估算 tokens。简短的成功标志、路径、摘要或诊断也可能承载重要信息，字符数量不能证明这些结果价值低。

**#49 决策：保留默认 512，保留现有参数，不引入聚合分支。** 128 只是本实验中能覆盖 491 净收益样例的配置值，不是所有用户或模型的推荐值。已有参数允许按工作负载选择更低的正整数门槛；配置 1 也能覆盖微小输出样例。聚合原型额外提供了批次总收益门槛，但本次没有真实任务质量证据支持增加这层行为或修改默认清理程度。输出很小或保护、输入和结构占主导时，降低门槛仍可能无法达标。

本交付没有写入任何用户配置，也没有为保留现状新建实现议题。若后续有代表性任务证据证明现有参数无法表达必要行为，再评估独立实现。

## 媒体：可以清理旧文本，但请求总预算仍然未知

每个样例在用户消息中保留一张 PNG，并有 20、100 或 300 个各输出 8,000 ASCII 字符的工具步骤。图片仍是完整的 `file` / `image/png` 内容。

当前引擎在这些场景的普通及 `force` 请求中均返回 `unknown-content`，折叠数为 0，输入输出副本相同。估算前后都未知，`overBudget` 为 `true`。其中生产统计的 `protectedSteps: 0` 表示在规划近期范围之前已经跳过，**不表示近期保护失效**；实验另行核对最后八个完整步骤始终保留。

离线原型仅针对可识别的用户 PNG：复制一份只用于规划已知文本的视图，把该图片暂换成空文本，调用现有引擎选候选，随后只将所选工具的新增 `compacted` 标记复制回包含原始图片的完整请求。规划视图从不提交给宿主。

这种方法没有给图片定价。原型完整请求的 `estimatedBefore`、`estimatedAfter`、`savedTokens` 均保持未知，`overBudget` 始终为 `true`；表中的收益只表示**已知文本部分的估算变化**。

| 步数 | 模式   | 默认折叠数 | 原型折叠数 | 已知文本估算节省 | 规划视图剩余估算 | 完整请求预算 |
| ---- | ------ | ---------: | ---------: | ---------------: | ---------------: | ------------ |
| 20   | normal |          0 |          7 |           13,937 |           27,188 | 未知         |
| 20   | force  |          0 |         12 |           23,892 |           17,233 | 未知         |
| 100  | normal |          0 |         90 |          179,190 |           26,335 | 未知         |
| 100  | force  |          0 |         92 |          183,172 |           22,353 | 未知         |
| 300  | normal |          0 |        292 |          581,372 |           35,353 | 未知         |
| 300  | force  |          0 |        292 |          581,372 |           35,353 | 未知         |

100 步样例确有大量旧文本可清理，但无法据此断言完整请求达到 28,000 的目标，更不能推断低于模型硬窗口。300 步即使只看规划视图仍超出目标。原型也没有解决图片与被清理文本之间的语义依赖。

额外对照分别把附件放在 `tool.state.attachments`、`tool.attachments` 和 `tool.state.metadata.attachments`。普通及 `force` 模式下，现行策略和原型都保持 0 折叠；原型不会将陌生工具附件当作已识别用户图片或假定为零成本。

**#48 决策：维持现行整次放行策略，不将旧文本原型接入生产。** 已知文本节省不等于未知媒体请求的可靠预算；本次也没有真实多模态任务成功率、遗漏重要图文关系或摘要语义保真的证据。当前缺少扩大自动剪枝范围的充分依据。

## 保护与失败边界

82 次投影全部执行以下断言，而不只比较节省数字：

- 原始历史与存储快照逐字段相同；返回独立副本。
- 每条消息、原始 part、调用 ID、顺序、输入、输出正文、错误、文本、推理、图片、已有宿主标记均保留；移除新增标记后与原始对象深度相等。
- 最近至少四步及 16,000 估算 tokens 的完整步骤保留，包括同一步的并行兄弟工具。
- 指令文件、动态加载指令、skill/task/dcp_prune、自定义未知工具、运行中/待运行/失败工具、失败或缺少成功退出证据的 bash、带错误的完成状态、配置保护的 grep 和已有宿主清理标记保持不变。
- 保护内容不足以释放目标容量时允许仍然超预算；不降低保护换取达标。

这验证了实验投影的结构和选择边界，不能证明删除旧结果之后真实模型仍会作出同样判断。

本实验没有安装媒体原型，也没有改变原生摘要入口。原始存储仍保留不等于已经验证原型接入后的 hook 顺序、摘要请求和执行生命周期；若以后采用原型，这些仍需独立实测。

本轮 [硬窗口验证 #46](https://github.com/LeXwDeX/OpenCode-Dynamic-Context-Pruning/issues/46) 在固定宿主 `8d9972908c308da1836a004cebe27c7c23db1acc` 的真实 HTTP 路径上使用统一 60,000 输入上限：普通工具输出对照没有拒绝；受保护指令输出收到两次真实 HTTP 400，经过两次宿主原生摘要后完成全部 13 个工具，副作用 ledger 只执行一次；不可装入的原始用户文本与摘要均被拒绝后以 `ContextOverflowError` 明确终止。这说明该固定实现有恢复和失败终态，不能推定用户实际发布宿主或任意真实模型都具有相同行为；发布兼容边界仍以 [#45](https://github.com/LeXwDeX/OpenCode-Dynamic-Context-Pruning/issues/45) 的独立证据为准。

[并行工具验证 #47](https://github.com/LeXwDeX/OpenCode-Dynamic-Context-Pruning/issues/47) 的四个独立场景及整套 14 项固定宿主集成测试均已通过。覆盖模式需要分别看待：上下文压力场景使用 Native LLM；成功、混合结果和取消场景使用 AI SDK。Native LLM 会缓冲整批工具调用，因此本轮没有证明该模式下“取消时某个兄弟工具已经较早完成”的行为。

这些宿主结果补足了执行边界证据。本实验的并行兄弟结果只是静态消息，不能代替真实宿主验证；两类证据也都不构成多模态或小输出清理后真实模型语义质量的证明。因此 #48/#49 以保留现有策略、记录收益和限制结案，不增加未经质量验证的生产分支。
